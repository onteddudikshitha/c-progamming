#include<stdio.h>
int main()
{
    int i,n,factorial=1;
    printf("enter n value:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        factorial=factorial*i;
    }
    printf("factorial=%d",factorial);
}
