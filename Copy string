#include<stdio.h>
#include<string.h>
int main()
{
    char  str1[15],str2[15];
    int i;
    printf("enter string:");
    scanf("%s",str1);
    for(i=0;str1[i]!='\0';i++)
    {
        str2[i]=str1[i];
    }
    str2[i]='\0';
    printf("after copying string is=%s",str2[i]);

}
