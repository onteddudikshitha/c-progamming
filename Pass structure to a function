#include<stdio.h>
#include<string.h>
struct employee
{
    int  id;
    char name[30];
};
void details(struct employee e[3]);
int main()
{
    struct employee e[3];
    int i,x;
    for(i=0;i<3;i++)
    {
        printf("id=%d\n",e[i].id);
        printf("name=%s\n",e[i].name);
    } 
    details(e);
}
void details(struct employee e[3])
{
    int i;
    for(i=0;i<3;i++)
    {
       printf("enter details \n");
       printf("enter id:");
       scanf("%d",&e[i].id);
       printf("enter name:");
       scanf("%s",e[i].name);
    }
}
