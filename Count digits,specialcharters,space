#include<stdio.h>
#include<string.h>
int main()
{
    char  str1[15];
    int i,digit=0,space=0,specialcharacter=0,Alphabet=0;
    printf("enter string:");
    gets(str1);
    for(i=0;str1[i]!='\0';i++)
    {
        if(str1[i]>='0' && str1[i]<='9')
            {
                digit++;
            }
        else if(str1[i]==' ')
        {
            space++;  
        } 
           else if((str1[i] >= 'a' && str1[i] <= 'z') ||
                (str1[i] >= 'A' && str1[i] <= 'Z'))
        {
             Alphabet++
        }
        else
        {
           specialcharacter++;
        }
    }
    printf("digit=%d",digit);
    printf("space=%d",space);
    printf("specialcharacter=%d",specialcharacter);
    printf("alphabet=%d",alphabet);
    
}
