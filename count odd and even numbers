#include<stdio.h>
int main()
{
    int i,even=0,odd=0,a[9],n;
    printf("enter n value:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a[i]%2==0)
        {
            even++;
        }
        else
        {
            odd++;
        }
    }
    printf("even=%d\n",even);
    printf("odd=%d",odd);
}
