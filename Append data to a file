#include<stdio.h>
#include<stdlib.h>
int main()
{
    FILE *fp;
    char str[100];
    fp=fopen("student.txt","a");
    if(fp==NULL)
    {
        printf("file is not created");
    }
    fprintf(fp,"chinnu");
    fclose(fp);
    printf("apendded successfully");
}
