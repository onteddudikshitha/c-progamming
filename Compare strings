#include<stdio.h>
#include<string.h>
int main()
{
    char str1[10]={"dikshi"};
    char str2[10]={"chinnu"};
    int i=0,same=1;
    while(str1[i]!='\0' && str2[i]!='\0')
    {
        if(str1[i]!=str2[i])
        {
           same=0;
           break;
        }
        i++;       
    }
      if(str1[i] != str2[i])
    {
        same = 0;
    }
    if(same==1)
    {
        printf("strings are  same");
    }
    else
    {
        printf("strings are not same");
    }
}
